<?xml version="1.0" ?>
<ruleset name="StyleGuide">
    <!-- Use the VIISON ruleset that's part of this repository -->
    <rule ref="./php/php-codesniffer-standard/VIISON">
        <include-pattern>*.php</include-pattern>
    </rule>
    <!-- Exclude some tests (since they actually contain wrong code style) -->
    <exclude-pattern>./php/php-codesniffer-standard/VIISON/Tests/*.inc</exclude-pattern>
</ruleset>
